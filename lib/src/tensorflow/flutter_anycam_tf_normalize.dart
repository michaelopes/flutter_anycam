enum FlutterAnycamTfNormalize {
  centered("centered"),
  custom("custom"),
  imageNet("imageNet"),
  simple("simple"),
  bgr("bgr"),
  none("none");

  final String value;
  const FlutterAnycamTfNormalize(this.value);
}
