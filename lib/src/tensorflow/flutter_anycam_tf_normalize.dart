enum FlutterAnycamTfNormalize {
  centered("centered"),
  custom("custom"),
  imageNet("imageNet"),
  simple("simple"),
  none("none");

  final String value;
  const FlutterAnycamTfNormalize(this.value);
}
